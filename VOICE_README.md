# 岐黄讲解员

Windows / Python 3.11+，使用标准库运行，无需 ROS2 或额外 pip 依赖。

## 启动与配置

在本目录的 PowerShell 中运行 `./start_qihuang_voice.ps1`，然后打开 http://127.0.0.1:8010/ 。
首次启动自动从 `.env.example` 创建 `.env`。仅在本机编辑 `.env` 中的 `OPENAI_API_KEY=`，填入自己的 OpenAI API Key，保存后点击网页“检查配置”，无需重启服务。

“已配置”只表示检测到密钥，不代表连接、余额或模型权限已经验证。首次发送问题会实际调用 API；录音转写、问答、语音合成都按 OpenAI 的实际用量计费。默认不自动重试付费调用。环境变量优先于 `.env`。

API Key 仅由后端读取，不返回给浏览器。`.env` 已被 Git 忽略；静态服务只暴露 `dist/`。请勿上传 `.env`，也不要把密钥粘贴到聊天中。

默认模型：`gpt-4.1-mini`（问答）、`gpt-4o-transcribe`（中文转写）、`gpt-4o-mini-tts`（语音）。模型及音色可在 `.env` 调整；需确保账号有对应模型权限。

## 使用

- 点击“开始说话”，授权成功后开始录音；点击“说完了”后转写并回答。转写结果保留在输入框，可修正重发。
- 默认手动结束，最多 45 秒；可选择停顿 2/3/5 秒后结束。现场背景噪声可能影响静音判断。
- 点击“停止讲解 / 取消”停止本地播放、录音和等待。已经发往 OpenAI 的请求可能仍产生用量；迟到结果不再显示或播放。
- “新对话”清空本页历史；刷新也会清空。最多带入最近 6 轮对话，不把原始录音写入磁盘。
- “静音”跳过语音合成；“重播回答”使用后端内存缓存，最多缓存 32 条音频，重启后清空。
- 当前穴位、页面模式及对话一起发送给模型。普通的“看看左天宗”只切换网页；模型的机械臂建议必须经过原有确认按钮。
- 音频、问题和上下文会经本机服务发送到 OpenAI。Responses 调用设置 `store:false`；不等同于服务商端零保留承诺。
- 当前知识文件从项目原有页面整理，明确标注为待权威校订的科普资料。不会把它包装成已经核验过的古籍或医学证据。

## 与机器人连接

语音网页在 8010 端口独立运行。若已按原有流程在本机 8008 启动机器人平台，新服务会代理原有 `/status.json`、视频和限定的机器人接口。语音服务不会启动机器人、修改驱动或自动执行模型提议。

原 `start_qihuang_platform.ps1` 仍用于启动机器人平台；语音升级版从 `start_qihuang_voice.ps1` 打开。独立服务当前监听 IPv4 回环地址，仅供本机使用。

停止语音服务：`./stop_qihuang_voice.ps1`。

## 验证

运行 `python -m unittest discover -s voice_backend/tests -v` 做不连接外部 API 的接口测试。安装 Node.js 时还可运行 `node --test voice_backend/tests/voice_ui.test.cjs`，验证取消请求、上下文切换、三轮对话和麦克风释放的异步行为。模型输出、错误、录音上传和音频缓存采用模拟服务验证；真实识别准确率、音色、网络延迟及账号权限必须填写 Key 后实测。

参考：[Responses](https://developers.openai.com/api/docs/guides/text)、[语音识别](https://developers.openai.com/api/docs/guides/speech-to-text)、[语音合成](https://developers.openai.com/api/docs/guides/text-to-speech)。

### 2026-09-17 本机真实联调

- 真实问答成功，首个后端请求约 4.03 秒。
- 生成测试句“你好，欢迎来到岐黄巧手。我们今天介绍大椎、天宗和肾俞。”的 MP3，约 3.56 秒；将该合成音频送入转写，约 3.86 秒，三个穴位名称均正确（标点略有变化）。这不是现场麦克风或噪声准确率测试。
- 浏览器真实问答与播放成功，首轮文字提交至触发音频播放约 7.5 秒；三轮追问能保持大椎上下文，再按要求切换左天宗并比较位置。第二、第三轮文字回答分别约 1.9、3.1 秒。
- 单次观测仅反映当时网络、模型与内容，不代表平均或保证延迟。现场收音、音色主观试听和机器人硬件链路仍需在相应环境测试。
- 16 项后端测试通过；连接状态提示修正后，8 项前端异步测试再次通过。

## 豆包 Vivi 2.0 音色

在豆包语音新版控制台的同一项目中开通语音合成模型 2.0，并创建语音 API Key：
https://console.volcengine.com/speech/new/setting/apikeys

将 Key 填入本机 `.env` 的 `DOUBAO_TTS_API_KEY=` 后面。填写后新生成的语音自动使用豆包，问答和识别继续使用 OpenAI；清空该字段即可恢复 OpenAI 音色。网页点击“检查配置”更新显示，无需重启。默认声音为 Vivi 2.0（`zh_female_vv_uranus_bigtts`）。接口使用官方 V3 SSE，当前后端收齐音频后再交给浏览器播放。费用和试用额度以控制台为准。豆包故障会显示错误并保留文字，不会自动切回另一家计费。

凭证只留在本机服务，不写入网页。当前已通过模拟响应验证鉴权字段、音频解析、错误处理及缓存隔离；尚未使用真实豆包凭证验证或试听。音色听感不保证与豆包 App 完全相同。

接口依据：
https://github.com/bytedance/agentkit-samples/blob/main/skills/byted-text-to-speech/scripts/text_to_speech.py
